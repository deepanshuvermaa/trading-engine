# Local environments / caches
venv/
.venv/
__pycache__/
**/__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.hf-cache/
.git/
.gitignore

# Local market-data cache and generated report DATA (rebuilt at runtime).
# NOTE: exclude only the runtime data subdirs, NOT the reports/ python package
# (reports/research_note.py, daily_report.py must ship in the image).
data/storage/
**/*.parquet
reports/audit/
reports/daily/
reports/simulation/
reports/live/

# Docs and local-only files
docs/
*.md
.dockerignore
Dockerfile
railway.toml
Procfile
